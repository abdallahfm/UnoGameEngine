package cardLogic;

import actionLogic.Action;

public class customCard extends Card{
    public customCard(String color, String value, int score, Action action) {
        super(color, value, score, action);
    }
}
