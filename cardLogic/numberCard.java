package cardLogic;

import actionLogic.Action;

public class numberCard extends Card{
    public numberCard(String color, String value, int score, Action action) {
        super(color, value, score, action);
    }
}
